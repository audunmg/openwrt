#!/bin/sh /etc/rc.common

START=00
USE_PROCD=1
NAME=qcontrol
PROG=/usr/sbin/qcontrol

start_service() {
	procd_open_instance
	procd_set_param command /usr/sbin/qcontrol -f -c /etc/qcontrol.conf
	procd_set_param stdout 1
	procd_close_instance
}
